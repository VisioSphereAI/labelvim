git workflow
To build python packages for Linux,
To build python package from Windows

To push it to pypi repository
To push it to anaconda repository

adding mask view, without mask view, object wise mask enable, or disable
Automatic version assign, when git tag applied


